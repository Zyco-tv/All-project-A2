{
  "name": "mastermind-react",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.10.2",
    "react-dom": "^16.10.2",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "tailwind": "tailwind build src/index.src.css -c tailwind.config.js -o src/index.css",
    "start": "npm run tailwind && react-scripts start",
    "build": "npm run tailwind && react-scripts build",
    "test": "npm run tailwind && react-scripts test",
    "eject": "npm run tailwind && react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "tailwindcss": "^1.1.2"
  }
}
